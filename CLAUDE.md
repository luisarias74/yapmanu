# LaLanguish Working Rules

This directory is the only source of truth for LaLanguish:

`/Users/luisarias/Documents/New project/LaLanguish`

Rules:
- Make all LaLanguish edits only in this folder.
- Do not create or use duplicate LaLanguish project copies elsewhere.
- Commit every meaningful change so the app can be rolled back safely.
- Keep backups outside this folder if needed, but do not treat them as active project copies.
